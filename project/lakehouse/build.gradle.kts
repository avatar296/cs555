/*
 * Lakehouse Parent Project
 *
 * This is an empty parent project that groups the medallion architecture modules:
 * - streaming: Shared lakehouse utilities
 * - schema-management: Table creation and schema management
 * - bronze: Raw data ingestion
 * - silver: Cleaned and transformed data
 * - gold: Analytics-ready aggregations
 *
 * No code lives in this parent project - all functionality is in subprojects.
 */
